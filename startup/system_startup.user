#!/usr/bin/env bash

# execute `crontab` as a non-root user
# add the line:
# @reboot /home/codeman/.itsoflife/mydata/git/bin-git/startup/system-startup.user

MYDATA=/home/codeman/.itsoflife/mydata;

echo "$(date) system-startup.user" >> $MYDATA/local/logs/mysystem.log;

#add the tasks to execute below
